#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    printf("printf() function belongs to stdio.h header file\n");
    printf("You need to include \"stdio.h\" using syntax: #include<stdio.h>\n");
    printf("There are many format specifiers in the printf()\n");
    printf("\t%%d is the format specifier for integer(decimal)\n");
    printf("\t%%f is the format specifier for float(real numbers)\n");
    printf("\t%%c is the format specifier for a character\n");
    printf("\t%%s is the format specifier for string");
                                      
    return 0;
}
